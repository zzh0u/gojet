# 应用配置
app:
  name: "gojet"
  version: "1.0.0"
  port: 8080
  mode: "debug"  # 运行模式: debug/release/test

# 数据库配置
database:
  host: "localhost"
  port: 5432
  user: "zhou"
  password: "password_"
  dbname: "gojet"
  sslmode: "disable"

# 日志配置
logging:
  level: "debug"  # 日志级别: debug/info/warn/error
  output: "stdout"  # 日志输出: stdout,file,both (开发环境用stdout,生产环境建议both)
  file_path: "./logs/app.log"  # 日志文件路径（当output为file或both时生效）

# JWT 配置
jwt:
  secret: "jwt 字符串，建议使用 openssl rand -base64 64 生成"
  access_expire_hours: 24  # Access Token 过期时间（小时）
  refresh_expire_days: 7   # Refresh Token 过期时间（天）

# Redis 配置
redis:
  host: "localhost"
  port: 6379
  password: ""  # 无密码留空
  db: 0         # Redis 数据库编号（0-15）